package com.forja.domain;

/** Categoria de treino — usada pelo gerador para equilibrar as rotinas. */
public enum ExerciseCategory {
    FORCA,
    PLIOMETRIA,
    CORE,
    CONDICIONAMENTO,
    MOBILIDADE,
    ESPECIFICO
}
